# ============================================================
# AgentMart — runtime dependencies (lean, verified installable)
# Optional extras (redis, postgres driver, LLM) are NOT required
# for local development. See README "可选增强".
# ============================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.1
pydantic-settings==2.3.4
sqlalchemy==2.0.30
aiosqlite==0.20.0
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.3.0
cachetools==5.3.3

# ---- 个人浏览器版（不装也能启动服务，只是浏览器任务不可用）----
playwright>=1.49,<2

# ---- dev / test ----
pytest==8.2.2
pytest-asyncio==0.23.7
